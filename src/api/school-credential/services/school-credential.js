'use strict';

/**
 * school-credential service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::school-credential.school-credential');
