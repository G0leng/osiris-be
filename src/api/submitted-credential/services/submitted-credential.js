'use strict';

/**
 * submitted-credential service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::submitted-credential.submitted-credential');
